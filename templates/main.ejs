<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SNS Main Page</title>
    <link rel="stylesheet" href="../static/styles/header.css"> <!-- 헤더 스타일을 포함한 CSS 파일 -->
    <link rel="stylesheet" href="../static/styles/footer.css"> <!-- 푸터 스타일을 포함한 CSS 파일-->
    <style>

        @font-face {
            font-family: 'neon';
            src: url('/static/styles/font/Pretendard-ExtraLight.otf') format('truetype');
        }

        @font-face{
            font-family: 'neon2';
            src: url('/static/styles/font/Pretendard-ExtraBold.otf') format('truetype');
        }

        body {
            font-family: 'neon';
            background-color: #f9f9f9;
            margin: 0;
            padding: 0;
            padding-bottom: 50px;
        }
        .container {
            display: grid;
            grid-template-columns: 15% 75%;
            gap: 20px;
            padding: 0px 20px 20px 20px;
        }
        .content-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            grid-template-rows: auto auto;
            gap: 20px;
        }
        .content {
            background-color: #ffffff;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 5px rgba(0,0,0,0.1);
        }
        .content h2 {
            background-color: #d7d7db;
            padding: 20px;
            margin: 0 0 10px 0;
            position: relative; /* 포지셔닝 컨텍스트 생성 */
            border-radius: 10px; /* 모서리 둥글게 만들기 */
            background-image: url('/static/pattern.svg'); /* SVG 패턴 파일 경로 */
        }
        .content h2 span {
            cursor: pointer;
        }
        .content h2 span:hover {
            color: #007bff;
            cursor: pointer;
        }
        .content h2 .text-container {
            font-family: 'neon2';
            position: relative;
            right: -30px; /* 오른쪽으로 10px 이동 */        
        }
        .post {
            background-color: white;
            margin: 10px 0;
            padding: 10px;
            border-radius: 5px;
            box-shadow: 0 0 5px rgba(0,0,0,0.1);
        }
        .post h3 {
            margin: 0;
            margin-left: 5px;
            font-weight: 100;
        }
        .post h4 {
            margin: 5px;
        }
        .post p {
            margin: 5px 0;
        }
        .post a {
            font-size: 25px;
            color: black;
            text-decoration: none;
        }
        .post a:hover {
            color: #007bff;
        }
        .post .info {
            display: flex;
            justify-content: space-between;
        }   


        .sidebar {
            background-color: #f9f9f9;
            padding: 10px;
            position: relative;
            top: -230px;
        }
        .top-writers {
        list-style-type: none;
        padding: 0;
        margin: 0;
        }
        .top-writers li {
        display: flex;
        align-items: center;
        margin-bottom: 10px;
        }
        .top-writers img {
            width: 30px;
            height: 30px;
            border-radius: 50%;
            margin-right: 10px;
        }
        .writer-info {
            display: flex;
            justify-content: space-between;
            width: 100%;
        }
        .writer-name {
            font-weight: bold;
        }
        .writer-points {
            display: flex;
            align-items: center;
            color: gray;
        }
        .writer-points img {
            width: 16px;
            height: 16px;
            margin-right: 5px;
        }
        .sidebar hr {
            border: none;
            border-top: 1px solid #ccc;
            margin: 10px 0;
        }
    </style>
    <script>
        function toggleDropdown() {
            const dropdown = document.getElementById('dropdown-menu');
            dropdown.style.display = dropdown.style.display === 'block' ? 'none' : 'block';
        }
        function goToBoard(category) {
            window.location.href = `/board/${category}`;
        }
    </script>
</head>
<body>
    <%- include('header') %> 
    <div class="banner" style="left:550px; position:relative;">
        <img src="/static/buss banner.png" alt="" style="height: 100px%; width: 700px;">
    </div>
    <div class="container">
        <!-- <div class="sidebar">
            <h3>Top Writers</h3>
            <ol style="margin: -15px -20px">
                <% if (typeof topwriters !== 'undefined') { %>
                    <% topwriters.forEach(writer => { %>
                        <li><strong><%= writer.username %></strong> <span style="color:gray;"><%= writer.postCount %>점</span></li>
                    <% }) %>
                <% } else { %>
                    <li>No top writers available.</li>
                <% } %>
            </ol>
        </div> -->

        <div class="sidebar">
            <h3>Top Writers</h3>
            <hr>
            <ol class="top-writers">
                <% if (typeof topwriters !== 'undefined') { %>
                    <% topwriters.forEach(writer => { %>
                        <li>
                            <img src="<%= writer.avatar %>" alt="<%= writer.username %>">
                            <div class="writer-info">
                                <span class="writer-name"><%= writer.username %></span>
                                <span class="writer-points">
                                    <img src="/static/trophy-icon.png" alt="Trophy Icon">
                                    <%= writer.postCount %>
                                </span>
                            </div>
                        </li>
                    <% }) %>
                <% } else { %>
                    <li>No top writers available.</li>
                <% } %>
            </ol>
        </div>

        <div class="content-grid">
            <div class="content">
                <h2> <span class="text-container">WEEKLY BEST</span>
                    <img src="/static/weekly-best.svg" alt="SVG Image" class="group_svg" >
                </h2>
                <% weeklyBestPosts.forEach(post => { %>
                <div class="post">
                    <div class="info">
                        <h3><%= post.author.name %></h3>
                        <span>💕<%= post.likes %> 🗨️ <%= post.comments.length %></span>
                    </div>
                    <h4><a href="/showPost/<%= post._id %>"><%= post.title %></a></h4>
                </div>
                <% }) %>
            </div>
            <div class="content">
                <h2><span onclick="goToBoard('community')"  class="text-container">COMMUNITY</span>
                <img src="/static/devops.svg" alt="SVG Image" class="group_svg" style="width: 150px; height: 150px; top:-42px;">
                </h2>
                <% communityPosts.forEach(post => { %>
                <div class="post">
                    <div class="info">
                        <h3><%= post.author.name %></h3>
                        <span>💕<%= post.likes %> 🗨️ <%= post.comments.length %></span>
                    </div>
                    <h4><a href="/showPost/<%= post._id %>"><%= post.title %></a></h4>
                </div>
                <% }) %>
            </div>
            <div class="content">
                <h2>
                    <span onclick="goToBoard('frontend')" class="text-container">FRONTEND</span>
                    <img src="/static/frontend.svg" alt="SVG Image" class="group_svg" >
                </h2>
                <% frontendPosts.forEach(post => { %>
                <div class="post">  
                    <div class="info">
                        <h3><%= post.author.name %></h3>
                        <span>💕<%= post.likes %> 🗨️ <%= post.comments.length %></span>
                    </div>
                    <h4><a href="/showPost/<%= post._id %>"><%= post.title %></a></h4>
                </div>
                <% }) %>
            </div>
            <div class="content">
                <h2><span onclick="goToBoard('backend')"  class="text-container">BACKEND</span>
                    <img src="/static/backend.svg" alt="SVG Image" class="group_svg" style="width:130px; height:130px; top: -36px;">
                </h2>
                <% backendPosts.forEach(post => { %>
                <div class="post">
                    <div class="info">
                        <h3><%= post.author.name %></h3>
                        <span>💕<%= post.likes %> 🗨️ <%= post.comments.length %></span>
                    </div>
                    <h4><a href="/showPost/<%= post._id %>"><%= post.title %></a></h4>
                </div>
                <% }) %>
            </div>
        </div>
    </div>
    <%- include('footer') %> <!-- 푸터 포함 -->
</body>
</html>